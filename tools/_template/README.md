# _template

Template for a productivity tool.

## Folders

- `config/`: configuration files
- `input/`: source files used by the tool
- `output/`: files generated by the tool

## Run

```powershell
python .\scripts\run_tool.py _template
```

